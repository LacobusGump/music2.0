---
name: Landauer-scattering bridge — March 25, 2026
description: The convergence point. ln(p) is simultaneously Landauer cost, scattering frequency, and von Mangoldt weight. The spectral shift function IS the information cost ledger of prime decomposition.
type: project
---

## The convergence

Three threads from this project meet at one point:

1. **Environment Rigidity / Landauer**: minimum cost of distinguishing = kT ln 2 per bit. Every computation is prime decomposition.
2. **Rainbow operator arc**: five operators tried, all failed. Direct quantization works. The operator is a scattering matrix with Euler product.
3. **R-metric / compiler**: computation cost = gap between independent and correlated entropy. Savings = premature erasure eliminated.

## The connection

ln(p) appears identically in:
- Landauer cost of resolving prime p: kT ln(p)
- Scattering frequency of prime p: sin(t · ln(p))
- Von Mangoldt function: Λ(p^k) = ln(p)

The spectral shift function ξ(t) = N_smooth(t) + R_M(t) is the cumulative information cost of all prime distinctions up to scale t. Zeros are quantization thresholds — where the cost hits integer values.

## Why potential theory fails

Schrödinger operators process information additively (superposition). Prime decomposition is multiplicative (Euler product). The natural language is scattering/transfer, not eigenstates/modes. This is why V1-V4 all failed and the direct quantization (which uses the multiplicative structure directly) succeeded.

## The S-matrix

S(t) = S_smooth(t) · Π_p S_p(t)

- S_smooth = Berry-Keating (H=xp) + Gamma correction
- Each S_p is unitary: prime channels preserve probability
- Euler product factors (verified: additivity ratio 0.80-0.93)
- Sub-additivity = inter-prime interference (higher-order correlations)

**How to apply:** This bridge connects the entropy/computation work to the number theory work. Any future operator construction should respect the multiplicative/scattering structure, not force additive/potential structure.
