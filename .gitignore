# Environment (contains API key - NEVER commit)
.env

# Dependencies
node_modules/

# Logs (keep locally, don't flood repo)
.gump/logs/

# Temp files
.gump/.commit-msg-temp
*.log
.DS_Store
Thumbs.db

# Media (keep locally, don't bloat repo)
*.mp4
*.MOV
*.mov
*.m4v
*.avi
*.wav
*.aif
*.aiff

# Cowork docs (local only)
GUMP_*.md
GUMP_*.html


# Sensitive legal docs — never commit
office/01_LEGAL/EIN_*.pdf
office/01_LEGAL/Operating_Agreement_*.pdf
*.env
credentials*
data/

# Large media — don't track
*.MOV
*.mp4
*.wav
*.gif
*.png
!favicon.png
!apple-touch-icon.png
!og-image.png
!oracle_chain_poster.png

# Old backups
*_backup*
*_old*
